name: "exp007_dice_reg_05_simple_encoder_dropout_0.2"
description: "UNetSimpleEncoder + reg aug, dropout 0.2, trained with Dice loss"
tags: ["exp007", "unet_simple_encoder", "dice_loss", "regularized", "dropout_0.2"]

dataset: "configs/datasets/fives512_g.yaml"

output:
  dir: "outputs/experiments/exp007_dice_reg_05_simple_encoder_dropout_0.2"
  save_predictions: true

data:
  batch_size: 4
  num_workers: 2
  pin_memory: true
  
  augmentation:
    enabled: true
    horizontal_flip: 0.5
    vertical_flip: 0.2
    rotation90: true
    rotation_prob: 0.5
    brightness: 0.1
  
  preprocessing:
    normalize: true
    pad_to_multiple: 32

model:
  type: "UNetSimpleEncoder"
  in_channels: 1
  out_channels: 1
  depths: [32, 64, 128, 256, 512]
  final_activation: "sigmoid"
  dropout: 0.2

training:
  epochs: 35
  
  optimizer:
    type: "adam"
    learning_rate: 0.0001
    weight_decay: 0.0001
  
  scheduler:
    type: "cosine"
    min_lr: 0.000001
  
  loss:
    type: "dice"
    smooth: 0.000001
  
  metrics:
    - "dice"
    - "focal_tversky"
    - "auc"
  
  mixed_precision: false
  gradient_clip: 1.0
  
  validation:
    frequency: 1
  
  early_stopping:
    enabled: true
    patience: 15
    monitor: "val_dice"
    mode: "max"
  
  checkpoint:
    save_best: true
    save_last: true
    monitor: "val_dice"
    mode: "max"

logging:
  tensorboard: true
  log_images: true
  image_log_frequency: 1
  
  log_activations: true
  activation_log_frequency: 1
  activation_layers: "auto"

debug:
  profile_memory: false
  detailed_memory: true
  estimate_activations: true
  profile_training_step: false

seed: 42
device: "cuda"


