name: "exp006_focal_reg_01_baseline"
description: "Baseline UNet + dropout + augmentation, trained with Focal Tversky loss"
tags: ["exp006", "unet", "focal_tversky", "regularized"]

dataset: "configs/datasets/fives512_g.yaml"

output:
  dir: "outputs/experiments/exp006_focal_reg_01_baseline"
  save_predictions: true

data:
  batch_size: 4
  num_workers: 2
  pin_memory: true
  
  augmentation:
    enabled: true
    horizontal_flip: 0.5
    vertical_flip: 0.2
    rotation90: true
    rotation_prob: 0.5
    brightness: 0.1
  
  preprocessing:
    normalize: true
    pad_to_multiple: 32

model:
  type: "UNet"
  in_channels: 1
  out_channels: 1
  depths: [32, 64, 128, 256, 512]
  final_activation: "sigmoid"
  dropout: 0.2

training:
  epochs: 50
  
  optimizer:
    type: "adam"
    learning_rate: 0.0001
    weight_decay: 0.0001
  
  scheduler:
    type: "cosine"
    min_lr: 0.000001
  
  loss:
    type: "focal_tversky"
    alpha: 0.3
    beta: 0.7
    gamma: 1.333
    smooth: 0.000001
  
  metrics:
    - "dice"
    - "focal_tversky"
    - "auc"
  
  mixed_precision: false
  gradient_clip: 1.0
  
  validation:
    frequency: 1
  
  early_stopping:
    enabled: true
    patience: 15
    monitor: "val_focal_tversky"
    mode: "max"
  
  checkpoint:
    save_best: true
    save_last: true
    monitor: "val_focal_tversky"
    mode: "max"

logging:
  tensorboard: true
  log_images: true
  image_log_frequency: 1
  
  log_activations: true
  activation_log_frequency: 1
  activation_layers: "auto"

debug:
  profile_memory: false
  detailed_memory: true
  estimate_activations: true
  profile_training_step: false

seed: 42
device: "cuda"

